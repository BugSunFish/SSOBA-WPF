<Window x:Class="ssoba.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ssoba"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TextBox x:Name="textBox1" Text="{Binding Path=ViewData, Mode=TwoWay}" Margin="64,54,400,181" />
        <TextBox x:Name="textBox2" Text="{Binding Path=PropertyTwo, Mode=TwoWay}" Margin="400,54,89,181" />
    </Grid>
</Window>
